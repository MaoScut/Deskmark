import React from 'react';
import styles from './App.css';

const App = () => (
  <div className={styles.app}>
    <h2>Hello, this is an simple on react hmr, edit something in /src/components/hello/App.js, you will see the app updates without browser refresh!</h2>
  </div>
);

export default App;